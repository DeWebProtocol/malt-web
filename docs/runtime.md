# Runtime Model

The maintained runtime and repository boundary document now lives at
[Runtime and Repository Boundaries](/docs/runtime).

This compatibility page remains only so older links do not break. It does not
define a second runtime contract.
